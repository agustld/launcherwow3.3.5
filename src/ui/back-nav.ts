/** Botón Volver a la portada. */

export function renderBackButton(): string {
  return `
    <button type="button" class="btn-back-portada" data-nav="home" data-i18n="nav.back">
      Volver
    </button>
  `;
}
